---
export const prerender = true;

import Sheet from "@/layouts/Sheet.astro";
import { MomentumBoard } from "@/components/MomentumBoard";
import {
  BODY,
  BREADCRUMB,
  CAPTION,
  CRUMB,
  DATUM,
  FIELD,
  FIGURE,
  HEADING,
  LEAD,
  MEASURE,
  PANEL,
  PROSE_LINK,
  ROW,
  SECTION_ACTION,
  SECTION_HEADER,
  TITLE,
  VALUE,
} from "@/components/style-tokens";
import { CATEGORIES } from "@/data/categories";
import { weeklyPace } from "@/lib/momentum";
import { formatCompact } from "@/lib/star-insights";
import { staticApiBase } from "@/lib/static-api-base";

const apiBase = staticApiBase();
const site = (Astro.site ?? new URL("https://gitdebt.com")).href.replace(/\/$/, "");

type LeaderboardRow = {
  rank: number;
  repo: string;
  stars: number;
  velocity: number;
};
type LeaderboardResponse = {
  metric: string;
  page: number;
  per_page: number;
  window_days: number;
  repos: LeaderboardRow[];
};

async function fetchJson<T>(url: string): Promise<T | null> {
  for (let attempt = 0; attempt < 3; attempt += 1) {
    try {
      const res = await fetch(url, {
        headers: { accept: "application/json" },
        signal: AbortSignal.timeout(20_000),
      });
      if (res.ok) return (await res.json()) as T;
      if (res.status < 500 && res.status !== 429) return null;
    } catch {}

    if (attempt < 2) {
      await new Promise((resolve) =>
        setTimeout(resolve, 500 * 2 ** attempt),
      );
    }
  }
  return null;
}

async function fetchBoard(
  metric: "stars" | "velocity",
  window = 7,
): Promise<LeaderboardResponse | null> {
  // The deepest page the API allows. Depth is what makes the merge work: a
  // repository has to appear in more than one window before its windows can be
  // compared, and at the previous fifty the overlap was thin enough that most
  // rows had nothing to compare.
  return fetchJson<LeaderboardResponse>(
    `${apiBase}/api/leaderboard.json?metric=${metric}&window=${window}&per=100&page=0`,
  );
}

const [dailyBoard, weeklyBoard, monthlyBoard, starsBoard, totalAnalyzed] = await Promise.all([
  fetchBoard("velocity", 1),
  fetchBoard("velocity", 7),
  fetchBoard("velocity", 30),
  fetchBoard("stars"),
  (async () => {
    const result = await fetchJson<{ total?: number }>(
      `${apiBase}/api/sitemap/repos?page=0&per=1`,
    );
    return typeof result?.total === "number" ? result.total : null;
  })(),
]);

const mostStarred = starsBoard?.repos ?? [];

// One row per repository, carrying every window at once.
//
// The page used to render the same repositories in four separate lists with one
// number each, which is why it could never answer whether a repo was rising or
// fading. A window a repo is absent from stays `null` rather than 0: the API
// returns a top-N per window, so absence means "not ranked here", not "gained
// nothing".
type Merged = {
  repo: string;
  stars: number;
  d1: number | null;
  d7: number | null;
  d30: number | null;
};
const merged = new Map<string, Merged>();
const absorb = (
  rows: LeaderboardRow[] | undefined,
  field: "d1" | "d7" | "d30" | null,
) => {
  for (const row of rows ?? []) {
    const existing = merged.get(row.repo) ?? {
      repo: row.repo,
      stars: row.stars,
      d1: null,
      d7: null,
      d30: null,
    };
    existing.stars = Math.max(existing.stars, row.stars);
    if (field) existing[field] = row.velocity;
    merged.set(row.repo, existing);
  }
};
absorb(dailyBoard?.repos, "d1");
absorb(weeklyBoard?.repos, "d7");
absorb(monthlyBoard?.repos, "d30");
absorb(starsBoard?.repos, null);

// Ranked by the weekly rate, which is the window the page has always led with.
//
// A repository ranked in no velocity window at all is excluded: it reached this
// set only through the most-starred board, so it has no pace to be ranked by
// and every momentum column on its row would be a dash. Carrying it would cost
// a row of vertical space to say nothing. Its size still shows wherever it does
// have movement, and the stars column carries size for every row that stays.
const rankedRows = [...merged.values()]
  .filter((row) => row.d1 !== null || row.d7 !== null || row.d30 !== null)
  .sort((a, b) => {
    const delta = weeklyPace(b) - weeklyPace(a);
    return delta !== 0 ? delta : b.stars - a.stars;
  });
const loaded = dailyBoard !== null || weeklyBoard !== null || monthlyBoard !== null || starsBoard !== null;
const hasRows = rankedRows.length > 0;
const windowDays = weeklyBoard?.window_days ?? 7;

// The page's lede, its description and its first FAQ answer all describe the
// board the reader actually sees, so they are read off `rankedRows` — the array
// `MomentumBoard` renders — rather than off one window's own ordinal.
const lead = rankedRows[0] ?? null;
const runnerUp = rankedRows[1] ?? null;

// The tightest window the leading row is genuinely ranked in. A window it is
// absent from is "not ranked here", not zero, so it is never quoted.
// `window` is the phrase, not the number: `windowDays` is whatever the API
// reports and the daily fallback is literally 1, so a bare "in {n} days" prints
// "in 1 days" on any build where the leading row is ranked daily but not
// weekly. The lede, the meta description and the FAQ answer all read this.
const paceWindow = (days: number) => (days === 1 ? "1 day" : `${days} days`);
const leadPace =
  !lead
    ? null
    : lead.d7 !== null
      ? { count: lead.d7, window: paceWindow(windowDays) }
      : lead.d1 !== null
        ? { count: lead.d1, window: paceWindow(1) }
        : lead.d30 !== null
          ? { count: lead.d30, window: paceWindow(30) }
          : null;

const vsPair =
  lead && runnerUp
    ? [lead.repo, runnerUp.repo].sort((a, b) => (a < b ? -1 : 1))
    : null;
const vsLabel = lead && runnerUp ? `${lead.repo} vs ${runnerUp.repo}` : null;

// 100 most-starred rows are already fetched for the merge; 24 of them are worth
// rendering, and the section restores the `#most-starred` anchor.
const mostStarredShown = mostStarred.slice(0, 24);

const updatedDate = new Date().toLocaleDateString("en-US", {
  month: "long",
  day: "numeric",
  year: "numeric",
  timeZone: "UTC",
});
const year = new Date().getUTCFullYear();

const title = `Fastest-growing GitHub repos (${year}) · gitdebt`;
// `Seo` truncates at 160, and a long slug is what pushes the specific variant
// over: the generic sentence is the guaranteed-whole fallback rather than a
// description ending in an ellipsis.
const genericDescription =
  "Fastest-growing and most-starred GitHub repos, ranked from public star timestamps across every repo gitdebt has analyzed. Updated daily.";
const leadDescription =
  lead && leadPace
    ? `${lead.repo} leads: ${leadPace.count.toLocaleString()} stars in ${leadPace.window}. Fastest-growing and most-starred GitHub repos, ranked from public star timestamps.`
    : null;
const description =
  leadDescription && leadDescription.length <= 160
    ? leadDescription
    : genericDescription;

const canonicalPath = "/leaderboard";
const ogImage = `${apiBase}/api/og.png`;

const noindex = !loaded || !hasRows;

const featuredCategories = CATEGORIES.slice(0, 8);

const jsonLd: Record<string, unknown>[] = [
  {
    "@context": "https://schema.org",
    "@type": "WebPage",
    name: "GitHub repo leaderboard — fastest-growing & most-starred",
    url: `${site}/leaderboard`,
    description,
    dateModified: new Date().toISOString().slice(0, 10),
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: [
      { "@type": "ListItem", position: 1, name: "gitdebt", item: `${site}/` },
      { "@type": "ListItem", position: 2, name: "Leaderboard", item: `${site}/leaderboard` },
    ],
  },
];
// One list, over the array the board renders, positioned by rendered order. The
// per-window ordinal the API returns describes a different ordering than the
// merged board, so quoting it put positions on rows that do not hold them.
const rankedItems = rankedRows.slice(0, 25);
if (rankedItems.length > 0) {
  jsonLd.push({
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: "Every tracked repo, ranked by weekly pace",
    itemListOrder: "https://schema.org/ItemListOrderDescending",
    numberOfItems: rankedItems.length,
    itemListElement: rankedItems.map((row, index) => ({
      "@type": "ListItem",
      position: index + 1,
      name: row.repo,
      url: `${site}/${row.repo}`,
    })),
  });
}
if (mostStarredShown.length > 0) {
  jsonLd.push({
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: "Most-starred repos on gitdebt",
    itemListOrder: "https://schema.org/ItemListOrderDescending",
    numberOfItems: mostStarredShown.length,
    itemListElement: mostStarredShown.map((row, index) => ({
      "@type": "ListItem",
      position: index + 1,
      name: row.repo,
      url: `${site}/${row.repo}`,
    })),
  });
}
if (lead && leadPace && mostStarredShown.length > 0) {
  jsonLd.push({
    "@context": "https://schema.org",
    "@type": "FAQPage",
    mainEntity: [
      {
        "@type": "Question",
        name: "What is the fastest-growing repo on gitdebt right now?",
        acceptedAnswer: {
          "@type": "Answer",
          text: `${lead.repo} sets the fastest weekly pace gitdebt tracks, gaining ${leadPace.count.toLocaleString()} GitHub stars in the last ${leadPace.window} (as of ${updatedDate}). The full ranking is computed from public star timestamps.`,
        },
      },
      {
        "@type": "Question",
        name: "How is the gitdebt leaderboard computed?",
        acceptedAnswer: {
          "@type": "Answer",
          text: `Rankings come from public GitHub star timestamps cached by gitdebt${totalAnalyzed ? ` across the ${totalAnalyzed.toLocaleString()} repos it has analyzed` : ""}. "Fastest-growing" ranks by stars added in the trailing ${windowDays} days; "most-starred" ranks by current total stars. No repo is ranked until its star history is cached.`,
        },
      },
    ],
  });
}
---

<Sheet
  title={title}
  description={description}
  canonicalPath={canonicalPath}
  ogImage={ogImage}
  noindex={noindex}
  jsonLd={jsonLd}
>
  {/* ── The title block ──────────────────────────────────────────────────── */}
  <section class="registered border-b border-rule bg-paper">
    <div class="mx-auto w-full max-w-6xl px-6 pt-8 pb-14">
      <nav aria-label="Breadcrumb" class={BREADCRUMB}>
        <a href="/" class={CRUMB}>gitdebt</a>
        <span class="px-1.5 text-rule-strong" aria-hidden="true">/</span>
        <span class="text-ink">leaderboard</span>
      </nav>

      <div
        class="mt-6 grid gap-x-12 gap-y-8 lg:grid-cols-[minmax(0,1fr)_auto] lg:items-end"
      >
        <h1 class={TITLE}>Fastest-growing & most-starred repos</h1>

        {/* The lead row's pace, dimensioned: a field, its figure in drafting
            red, and the span the figure was measured over. It is the one
            measured value in this title block, so it is the only red on it. */}
        {lead && leadPace && (
          <div class="lg:text-right">
            <p class={FIELD}>Fastest weekly pace</p>
            <p class:list={["mt-2.5", VALUE, FIGURE]}>
              +{leadPace.count.toLocaleString()}
            </p>
            <p class:list={["mt-1.5", CAPTION]}>
              stars in {leadPace.window} · {lead.repo}
            </p>
          </div>
        )}
      </div>

      {lead && leadPace ? (
        <p class:list={["mt-6", LEAD, MEASURE]}>
          <a href={`/${lead.repo}`} class={PROSE_LINK}>{lead.repo}</a> sets the
          fastest weekly pace on the board. Rankings come from public star
          timestamps{totalAnalyzed
            ? ` across the ${totalAnalyzed.toLocaleString()} repos gitdebt has analyzed`
            : " across every repo gitdebt has analyzed"} — a repo joins once its
          star history is cached.
        </p>
      ) : (
        <p class:list={["mt-6", LEAD, MEASURE]}>
          Repos ranked by star growth and total stars, computed from public star
          timestamps across every repo gitdebt has analyzed.
        </p>
      )}

      <p class:list={["mt-4", CAPTION]}>
        Data updated {updatedDate} · rankings refreshed daily from cached public
        history
      </p>
    </div>
  </section>

  {/* ── The board ────────────────────────────────────────────────────────── */}
  <section id="ranking" class="scroll-mt-24 border-b border-rule bg-table">
    <div class="mx-auto w-full max-w-6xl px-6 py-16">
      {!loaded && (
        <div class:list={["max-w-2xl", PANEL]} role="alert">
          <p class={FIELD}>Leaderboard unavailable</p>
          <p class:list={["mt-2.5", BODY, MEASURE]}>
            We couldn't load the rankings. Try again in a moment.
          </p>
        </div>
      )}

      {loaded && !hasRows && (
        <div class:list={["max-w-2xl", PANEL]}>
          <p class={FIELD}>No ranking yet</p>
          <p class:list={["mt-2.5", BODY, MEASURE]}>
            {mostStarredShown.length > 0
              ? "Growth rankings are unavailable right now. The most-starred board below is current."
              : "No repos have a complete star history yet. Look up any repo to start tracking it — it joins the leaderboard as soon as its history is cached."}
          </p>
        </div>
      )}

      {loaded && hasRows && lead && (
        <Fragment>
          <div class={SECTION_HEADER}>
            <h2 class={HEADING}>Every tracked repo, ranked by weekly pace</h2>
            <p class={CAPTION}>{rankedRows.length} repositories</p>
          </div>
          <div class="mt-8">
            <MomentumBoard client:visible rows={rankedRows} />
          </div>
          <p class:list={["mt-5", CAPTION, MEASURE]}>
            Bar length is the weekly pace; the brighter tip is today's, on the
            same per-day scale — a short bar with a bright tip is a repo spiking
            right now. The direction mark compares today against the same repo's
            30-day pace: an arrowhead up is climbing, down is cooling, a rule is
            level. A dash means the repo is outside that window's top ranks, not
            that it gained nothing.
          </p>
          <p class:list={["mt-3", CAPTION, MEASURE]}>
            Rankings are computed from star timestamps whose source differs per
            repository; each report names its own — see <a
              href={`/${lead.repo}`}
              class={PROSE_LINK}>{lead.repo}</a
            >.{vsPair && vsLabel && (
              <Fragment>
                {" "}Or compare the top two directly: <a
                  href={`/vs/${vsPair[0]}/${vsPair[1]}`}
                  class={PROSE_LINK}>{vsLabel}</a
                >.
              </Fragment>
            )}
          </p>
        </Fragment>
      )}
    </div>
  </section>

  {/* ── Most starred ─────────────────────────────────────────────────────── */}
  {mostStarredShown.length > 0 && (
    <section id="most-starred" class="scroll-mt-24 border-b border-rule bg-paper">
      <div class="mx-auto w-full max-w-6xl px-6 py-16">
        <div class={SECTION_HEADER}>
          <h2 class={HEADING}>Most-starred repos</h2>
          <p class={CAPTION}>
            top {mostStarredShown.length} by current total stars
          </p>
        </div>
        <ul
          role="list"
          class="mt-8 grid gap-px border border-rule-strong bg-rule sm:grid-cols-2"
        >
          {mostStarredShown.map((row, index) => (
            <li class="bg-paper">
              <a href={`/${row.repo}`} class:list={[ROW, "justify-between"]}>
                <span class:list={[DATUM, "w-6 shrink-0 text-right text-ink-3"]}>
                  {index + 1}
                </span>
                <span class="min-w-0 flex-1 truncate">{row.repo}</span>
                <span class:list={[DATUM, "shrink-0 text-ink-3"]}>
                  <span class="sr-only">stars: </span>
                  {formatCompact(row.stars)}
                </span>
              </a>
            </li>
          ))}
        </ul>
      </div>
    </section>
  )}

  {/* ── Browse by category ───────────────────────────────────────────────── */}
  <section class="bg-table">
    <div class="mx-auto w-full max-w-6xl px-6 py-16">
      <div class={SECTION_HEADER}>
        <h2 class={HEADING}>Browse by category</h2>
        <a href="/compare#categories" class={SECTION_ACTION}>all categories</a>
      </div>
      <ul
        role="list"
        class="mt-8 grid gap-px border border-rule-strong bg-rule sm:grid-cols-2"
      >
        {
          featuredCategories.map((category) => (
            <li class="bg-paper">
              <a
                href={`/compare/${category.slug}`}
                data-warm-repos={category.repos.join(",")}
                class:list={[ROW, "min-h-16 justify-between py-2.5"]}
              >
                <span class="min-w-0 flex-1">
                  <span class="block truncate">{category.name}</span>
                  <span class:list={["mt-0.5 block truncate", CAPTION]}>
                    {category.short}
                  </span>
                </span>
                <span class:list={[DATUM, "shrink-0 text-ink-3"]}>
                  {category.repos.length}
                </span>
              </a>
            </li>
          ))
        }
      </ul>
      <p class:list={["mt-6", BODY, MEASURE]}>
        Or <a href="/compare" class={PROSE_LINK}
          >build your own star-history comparison</a
        >.
      </p>
    </div>
  </section>
</Sheet>
